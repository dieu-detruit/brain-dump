"""brain-dump-bridge: localhost daemon that turns a generic push into a Thread.

Kept intentionally thin — all Supabase knowledge lives in `brain-dump-sdk`;
this package is only the listener in front of it.
"""
